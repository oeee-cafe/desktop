{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The bundled loader page only: its strip where the toolbar will be drags the window (and maximises it on a double-click), and on Windows its caption buttons minimise, maximise and close it. Nothing else of Tauri's, and oeee.cafe itself is given no access to Tauri here.",
  "windows": ["main"],
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close"
  ]
}
