{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The bundled loader page only: its strip where the toolbar will be drags the window. oeee.cafe itself is given no access to Tauri here.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
