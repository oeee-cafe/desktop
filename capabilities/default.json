{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The bundled loader page only. oeee.cafe itself is given no access to Tauri.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
